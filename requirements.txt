groq>=0.11.0
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=20.1.0
pyyaml>=6.0
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0

# Note: Vosk removed (offline ASR) — add back if you re-enable speech features


groq>=1.1.0
duckduckgo-search>=6.0
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=20.1.0
pyyaml>=6.0
requests>=2.31.0
python-dotenv>=1.0.0

# Testing / dev dependencies (optional)
pytest>=8.0
pytest-cov>=4.0

# Voice mode dependencies
# edge-tts and deepgram-sdk removed completely since audio is now processed via Puter.js in the browser
